# day drinking
https://sebastianverjoustinsky-rgb.github.io/day-drinking/

If you edit `style.css` or `script.js`, bump the `?v=` number on both links
at the top and bottom of `index.html`. Otherwise people who already opened
the page keep the old version for ~10 minutes. Editing text in `index.html`
alone needs no bump.
